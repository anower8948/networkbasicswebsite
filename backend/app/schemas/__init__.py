"""Pydantic schemas — the API's request and response contracts.

Intentionally free of re-exports; see `app.services.__init__` for why. Import
concrete modules: `from app.schemas.user import UserRead`.
"""
